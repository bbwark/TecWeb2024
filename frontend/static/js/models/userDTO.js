export default class UserDTO {
    constructor(id, username, name, isAdmin) {
        this.id = id;
        this.username = username;
        this.name = name;
        this.isAdmin = isAdmin;
    }
}
